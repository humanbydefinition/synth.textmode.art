# synth.textmode.art

Small GitHub Pages redirect site that forwards `https://synth.textmode.art` to `https://editor.textmode.art`.

It preserves:

- path
- query string
- hash fragment

That keeps shared sketch URLs viable while the main app lives on `editor.textmode.art`.
